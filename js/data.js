// 🗂️ ডেমো/নমুনা রেকর্ড সরিয়ে দেওয়া হয়েছে — শুধু আপলোডকৃত PDF-এর ডেটাই থাকবে।
// রেকর্ড যোগ করতে অ্যাডমিন প্যানেল ব্যবহার করুন (js/data-uploads.js-এ সেভ হয়)।
const DATA = [];

const KHATIAN_DATA = [];
